apiVersion: v1
kind: Service
metadata:
  name: nginx
  namespace: namespace-wordpress
  labels:
    app: nginx
spec:
  ports:
  - name: http
    port: 80
    protocol: TCP
    targetPort: 80
  selector:
    app: wordpress
  type: NodePort
